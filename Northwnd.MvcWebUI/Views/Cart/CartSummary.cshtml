@model Northwnd.Entities.Cart

<div>
    <span>
        <b> Your Cart </b>
        @Model.Total.ToString("c")
    </span>
    @*@Html.ActionLink("Checkout","Index","Cart")*@
    <a href="/Cart/Index" class="btn btn-primary btn-sm">Sepet(@Model.Lines.Sum(x => x.quantity))</a>
</div>
